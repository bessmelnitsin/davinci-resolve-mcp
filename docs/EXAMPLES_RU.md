# Примеры использования (Usage Examples)

Практические сценарии использования DaVinci Resolve MCP Server.

---

## 🚀 1. Быстрый старт: Создание проекта

Сценарий: Создание нового проекта, настройка разрешения и формата.

```json
// 1. Создать проект
{
  "name": "create_project",
  "arguments": {
    "name": "My New Vlog"
  }
}

// 2. Открыть проект (если не открылся автоматически)
{
  "name": "open_project",
  "arguments": {
    "name": "My New Vlog"
  }
}

// 3. Настроить таймлайн (1920x1080, 24 fps)
{
  "name": "set_timeline_format_tool",
  "arguments": {
    "width": 1920,
    "height": 1080,
    "frame_rate": 24.0
  }
}
```

---

## 🎬 2. Импорт и организация медиа

Сценарий: Импорт видео, создание папок и сортировка.

```json
// 1. Создать папку для исходников
{
  "name": "create_bin",
  "arguments": {
    "name": "Raw Footage"
  }
}

// 2. Импортировать видео
{
  "name": "import_media",
  "arguments": {
    "file_path": "C:/Videos/Camera01.mp4"
  }
}

// 3. Переместить файл в папку
{
  "name": "move_media_to_bin",
  "arguments": {
    "clip_name": "Camera01.mp4",
    "bin_name": "Raw Footage"
  }
}
```

---

## 🤖 3. Умный монтаж (AI Editing)

Сценарий: Автоматическое удаление тишины и создание вертикальных Reels.

```json
// 1. "Jump Cut" - удалить тишину из длинного видео
{
  "name": "smart_jump_cut",
  "arguments": {
    "clip_name": "Long_Interview.mp4",
    "silence_threshold": 0.5
  }
}

// 2. Создать вертикальные Reels (9:16) из горизонтального видео
{
  "name": "viral_reels_factory",
  "arguments": {
    "clip_name": "Podcast_Full.mp4"
  }
}

// 3. Найти виральные моменты (без создания таймлайна)
{
  "name": "find_viral_segments",
  "arguments": {
    "clip_name": "Stream_Recording.mp4",
    "max_segments": 5,
    "language": "ru" // Важно указать язык для лучшего анализа
  }
}
```

---

## 📝 4. Работа с текстом и транскрибацией

Сценарий: Транскрибация видео для субтитров.

```json
// 1. Транскрибировать клип и получить текст
{
  "name": "transcribe_clip",
  "arguments": {
    "clip_name": "Interview.mp4",
    "language": "ru",
    "output_format": "srt" // Формат субтитров
  }
}

// 2. Транскрибировать всю папку (Native AI)
{
  "name": "transcribe_folder_tool",
  "arguments": {
    "folder_name": "Interviews"
  }
}
```

---

## 🎨 5. Цветокоррекция

Сценарий: Применение LUT и автоматическая настройка.

```json
// 1. Применить LUT
{
  "name": "apply_lut",
  "arguments": {
    "lut_path": "Film Looks/Rec709 Kodak 2383 D60.cube"
  }
}

// 2. Скопировать грейд с другого клипа
{
  "name": "copy_grade",
  "arguments": {
    "source_clip_name": "Reference_Shot.mp4",
    "target_clip_name": "Current_Shot.mp4"
  }
}
```

---

## 📤 6. Рендер (Delivery)

Сценарий: Добавление в очередь и рендер.

```json
// 1. Добавить в очередь рендера (YouTube 1080p)
{
  "name": "add_to_render_queue",
  "arguments": {
    "preset_name": "YouTube 1080p",
    "timeline_name": "Final Cut"
  }
}

// 2. Запустить рендер
{
  "name": "start_render",
  "arguments": {}
}

// 3. Проверить статус
{
  "name": "get_render_queue_status",
  "arguments": {}
}
```
