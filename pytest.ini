[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not live"
asyncio_mode = auto
markers =
    live: tests that require a running DaVinci Resolve; skipped by default
